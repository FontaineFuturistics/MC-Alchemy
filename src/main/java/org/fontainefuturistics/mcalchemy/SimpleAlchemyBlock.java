package org.fontainefuturistics.mcalchemy; // Declare our package

import net.minecraft.world.level.block.Block; // Import Block so I can extend it

/**
 * This is a simple custom block. If we wanted to have complex funtionality like block interactions, we would define it in here.
 */
public class SimpleAlchemyBlock extends Block {
    
    // Constructor
    public SimpleAlchemyBlock(Properties properties) {
        // Constructor stub
        super(properties);
    }

}
